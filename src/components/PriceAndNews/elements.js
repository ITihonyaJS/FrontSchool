import styled from "styled-components";

import {Wrapper} from "../../generalElements"

export const BlockContainer = styled.div`
background: #FFFFFF;
`
export const BlockBody = styled(Wrapper)`
padding: 700px 0px 50px;
position: relative;
`